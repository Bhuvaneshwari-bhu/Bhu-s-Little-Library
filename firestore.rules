rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /books/{bookId} {
      allow read: if true;               // anyone can view the books
      allow write: if request.auth != null; // only signed-in admin can add/edit/delete
    }
  }
}
